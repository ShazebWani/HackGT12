fastapi
uvicorn[standard]
python-multipart
websockets
databases[sqlite]
SQLAlchemy
python-dotenv

sounddevice
soundfile
numpy
openai

# keyboard  # Skip this for now - it installs many macOS frameworks
# SpeechRecognition  # Skip this for now
# pyaudio  # Skip this - requires system PortAudio library